/** @type {import('next').NextConfig} */
const nextConfig = {
  // Hide the dev-mode "N" route indicator (bottom-left badge with Route/Bundler/Preferences popover).
  devIndicators: false,
};

export default nextConfig;
